void get_prim(int num){
    cnt = 0;
    memset(v, 0, sizeof v);
    for(int i = 2; i <= num; i++){
        if(!v[i]) prim[cnt++] = i;
        for(int j = 0; j < cnt; j++){
            if(prim[j] > v[i] || prim[j] > num / i) break;
            v[prim[j] * i] = prim[j];
        }
    }
}